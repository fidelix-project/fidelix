# Chroot System Configuration (All Architectures)
#
# This system configuration is intended for use in a
# chroot/container/docker image. It installs only the userspace and skips all
# boot related packages.

# The packages in the boot group to build for this system
BOOT_PACKAGES=boot-scripts

