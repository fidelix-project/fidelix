# DO NOT EDIT THIS FILE
#
# Any manual changes to this file will be overwritten on system upgrades. To
# customize the ashrc, create the file /etc/ashrc.local, and place any
# additional commands in there.

# Fidelix Ashrc
# This script is sourced by ash (/bin/sh) for all shells.

# Adjust the PATH (Ash's default doesn't work well on Fidelix)
if [ $(id -u) -eq 0 ]; then
	export PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
else
	export PATH=/usr/local/bin:/usr/bin:/bin
fi

# Source the local ashrc (if any)
if [ -r /etc/ashrc.local ]; then
	source /etc/ashrc.local
fi

# Source the user ashrc (if any)
if [ -r "$HOME/.ashrc" ]; then
	source "$HOME/.ashrc"
fi

